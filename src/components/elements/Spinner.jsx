import React from 'react';

import { Glyphicon } from 'react-bootstrap';

const Spinner = () => (
  <div className="trips-spinner text-center spinner">
    <Glyphicon glyph="repeat normal-spinner" />
  </div>
);

export default Spinner;
